services:
  neo4j:
    image: neo4j:5
    container_name: socratic-pm-neo4j
    ports:
      - "7474:7474"
      - "7687:7687"
    environment:
      - NEO4J_AUTH=neo4j/password
      - NEO4J_PLUGINS=["apoc"]
    volumes:
      - ./.socratic-pm/graph:/data
    restart: unless-stopped
